import 'package:flutter/material.dart';

class storeDetailes extends StatelessWidget {
  const storeDetailes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
